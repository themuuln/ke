[package]
name = "ke"
version = "0.1.0"
edition = "2021"
description = "macOS Keychain secrets manager — minimal TUI + CLI"

[dependencies]
ratatui = { version = "0.30", features = ["crossterm"] }
crossterm = "0.28"
anyhow = "1"
rpassword = "7"

[dev-dependencies]
tempfile = "3"
